import { Then } from "@badeball/cypress-cucumber-preprocessor";

// most steps are shared in commom-step-definition.ts

// Then("eu consigo visualizar o texto {string}", (text: string) => {
//   cy.contains(text).should("be.visible");
// });
